<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the window before Compose draws; the Compose theme styles everything else. -->
    <style name="Theme.AllerScan" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/launcher_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
