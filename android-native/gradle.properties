org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# The AllerScan server the app talks to by default. 10.0.2.2 is the host machine as seen from the
# Android emulator, so `npm run dev` on the host works out of the box. Override it for a device
# build, for example: ./gradlew assembleDebug -Pallerscan.baseUrl=https://allerscan.example.com
allerscan.baseUrl=http://10.0.2.2:3000
